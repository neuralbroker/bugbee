[project]
name = "python-vuln"
description = "Synthetic fixture — intentional vulnerabilities for testing"

[hunt]
secrets = true
rules = true
aggressive = true

[policy]
redact_secrets = true
defense_only = true
