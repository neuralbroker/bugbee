---
title: Windows (WSL)
description: Execute o bugbee no Windows com WSL para melhor experiência.
---

import { Steps } from "@astrojs/starlight/components"

Embora o bugbee possa rodar direto no Windows, recomendamos usar [Windows Subsystem for Linux (WSL)](https://learn.microsoft.com/en-us/windows/wsl/install) para a melhor experiência. O WSL oferece um ambiente Linux que funciona de forma integrada com os recursos do bugbee.

:::tip[Por que WSL?]
O WSL oferece melhor desempenho de sistema de arquivos, suporte completo a terminal e compatibilidade com as ferramentas de desenvolvimento das quais o bugbee depende.
:::

---

## Configuração

<Steps>

1. **Instale o WSL**

   Se ainda não instalou, [instale o WSL](https://learn.microsoft.com/en-us/windows/wsl/install) usando o guia oficial da Microsoft.

2. **Instale o bugbee no WSL**

   Depois de configurar o WSL, abra o terminal do WSL e instale o bugbee usando um dos [métodos de instalação](/docs/).

   ```bash
   curl -fsSL https://github.com/neuralbroker/bugbee/install | bash
   ```

3. **Use o bugbee pelo WSL**

   Vá para o diretório do seu projeto (acesse arquivos do Windows via `/mnt/c/`, `/mnt/d/` etc.) e execute o bugbee.

   ```bash
   cd /mnt/c/Users/YourName/project
   bugbee
   ```

</Steps>

---

## App desktop + servidor WSL

Se você prefere usar o app desktop do bugbee, mas quer rodar o servidor no WSL:

1. **Inicie o servidor no WSL** com `--hostname 0.0.0.0` para permitir conexões externas:

   ```bash
   bugbee serve --hostname 0.0.0.0 --port 4096
   ```

2. **Conecte o app desktop** em `http://localhost:4096`

:::note
Se `localhost` não funcionar no seu ambiente, conecte usando o IP do WSL (no WSL: `hostname -I`) e use `http://<wsl-ip>:4096`.
:::

:::caution
Ao usar `--hostname 0.0.0.0`, defina `BUGBEE_SERVER_PASSWORD` para proteger o servidor.

```bash
BUGBEE_SERVER_PASSWORD=your-password bugbee serve --hostname 0.0.0.0
```

:::

---

## Cliente web + WSL

Para a melhor experiência web no Windows:

1. **Execute `bugbee web` no terminal WSL** em vez do PowerShell:

   ```bash
   bugbee web --hostname 0.0.0.0
   ```

2. **Acesse pelo navegador do Windows** em `http://localhost:<port>` (o bugbee mostra a URL)

Executar `bugbee web` a partir do WSL garante acesso correto ao sistema de arquivos e integração com o terminal, continuando acessível no navegador do Windows.

---

## Acessar arquivos do Windows

O WSL pode acessar todos os arquivos do Windows pelo diretório `/mnt/`:

- Unidade `C:` → `/mnt/c/`
- Unidade `D:` → `/mnt/d/`
- E assim por diante...

Exemplo:

```bash
cd /mnt/c/Users/YourName/Documents/project
bugbee
```

:::tip
Para uma experiência mais fluida, considere clonar/copiar seu repositório para o sistema de arquivos do WSL (por exemplo em `~/code/`) e executar o bugbee por lá.
:::

---

## Dicas

- Mantenha o bugbee rodando no WSL para projetos armazenados em unidades do Windows - o acesso aos arquivos fica fluido
- Use a [extensão WSL do VS Code](https://code.visualstudio.com/docs/remote/wsl) junto com o bugbee para um fluxo de desenvolvimento integrado
- Sua configuração e suas sessões do bugbee ficam armazenadas no ambiente WSL em `~/.local/share/bugbee/`
