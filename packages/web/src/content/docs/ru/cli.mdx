---
title: CLI
description: Параметры и команда bugbee CLI.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

CLI bugbee по умолчанию запускает [TUI](/docs/tui) при запуске без каких-либо аргументов.

```bash
bugbee
```

Но он также принимает команды, описанные на этой странице. Это позволяет вам программно взаимодействовать с bugbee.

```bash
bugbee run "Explain how closures work in JavaScript"
```

---

### tui

Запустите TUI bugbee.

```bash
bugbee [project]
```

#### Флаги

| Флаг                                     | Короткий | Описание                                                                      |
| ---------------------------------------- | -------- | ----------------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr> | `-c`     | Продолжить последний сеанс                                                    |
| <nobr><code>{"--session"}</code></nobr>  | `-s`     | Идентификатор сеанса для продолжения                                          |
| <nobr><code>{"--fork"}</code></nobr>     |          | Разветвить сеанс при продолжении (используйте с `--continue` или `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |          | Промпт для использования                                                      |
| <nobr><code>{"--model"}</code></nobr>    | `-m`     | Модель для использования в виде поставщика/модели.                            |
| <nobr><code>{"--agent"}</code></nobr>    |          | Агент для использования                                                       |
| <nobr><code>{"--port"}</code></nobr>     |          | Порт для прослушивания                                                        |
| <nobr><code>{"--hostname"}</code></nobr> |          | Имя хоста для прослушивания                                                   |

---

## Команды

CLI bugbee также имеет следующие команды.

---

### agent

Управляйте агентами для bugbee.

```bash
bugbee agent [command]
```

---

### attach

Подключите терминал к уже работающему внутреннему серверу bugbee, запущенному с помощью команд `serve` или `web`.

```bash
bugbee attach [url]
```

Это позволяет использовать TUI с удаленным сервером bugbee. Например:

```bash
# Start the backend server for web/mobile access
bugbee web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
bugbee attach http://10.20.30.40:4096
```

#### Флаги

| Флаг                                     | Короткий | Описание                                                                                         |
| ---------------------------------------- | -------- | ------------------------------------------------------------------------------------------------ |
| <nobr><code>{"--dir"}</code></nobr>      |          | Рабочий каталог для запуска TUI                                                                  |
| <nobr><code>{"--continue"}</code></nobr> | `-c`     | Продолжить последний сеанс                                                                       |
| <nobr><code>{"--session"}</code></nobr>  | `-s`     | Идентификатор сеанса для продолжения                                                             |
| <nobr><code>{"--fork"}</code></nobr>     |          | Создать ответвление сеанса при продолжении (используйте с `--continue` или `--session`)          |
| <nobr><code>{"--password"}</code></nobr> | `-p`     | Пароль базовой аутентификации (по умолчанию `BUGBEE_SERVER_PASSWORD`)                          |
| <nobr><code>{"--username"}</code></nobr> | `-u`     | Имя пользователя базовой аутентификации (по умолчанию `BUGBEE_SERVER_USERNAME` или `bugbee`) |

---

#### create

Создайте нового агента с пользовательской конфигурацией.

```bash
bugbee agent create
```

Эта команда поможет вам создать новый агент с настраиваемой системной подсказкой и настройкой инструмента.

---

#### list

Перечислите всех доступных агентов.

```bash
bugbee agent list
```

---

### auth

Команда для управления учетными данными и входом в систему для провайдеров.

```bash
bugbee auth [command]
```

---

#### login

bugbee использует список провайдеров с [Models.dev](https://models.dev), поэтому вы можете использовать `bugbee auth login` для настройки ключей API для любого поставщика, которого вы хотите использовать. Это хранится в `~/.local/share/bugbee/auth.json`.

```bash
bugbee auth login
```

Когда bugbee запускается, он загружает поставщиков из файла учетных данных. И если в ваших средах определены какие-либо ключи или файл `.env` в вашем проекте.

---

#### list

Перечисляет всех проверенных поставщиков, которые хранятся в файле учетных данных.

```bash
bugbee auth list
```

Или короткая версия.

```bash
bugbee auth ls
```

---

#### logout

Выключает вас из провайдера, удаляя его из файла учетных данных.

```bash
bugbee auth logout
```

---

### github

Управляйте агентом GitHub для автоматизации репозитория.

```bash
bugbee github [command]
```

---

#### install

Установите агент GitHub в свой репозиторий.

```bash
bugbee github install
```

Это настроит необходимый рабочий процесс GitHub Actions и проведет вас через процесс настройки. [Подробнее](/docs/github).

---

#### run

Запустите агент GitHub. Обычно это используется в действиях GitHub.

```bash
bugbee github run
```

##### Флаги

| Флаг                                  | Описание                                      |
| ------------------------------------- | --------------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | Имитирующее событие GitHub для запуска агента |
| <nobr><code>{"--token"}</code></nobr> | Токен личного доступа GitHub                  |

---

### mcp

Управляйте серверами протокола контекста модели.

```bash
bugbee mcp [command]
```

---

#### add

Добавьте сервер MCP в свою конфигурацию.

```bash
bugbee mcp add
```

Эта команда поможет вам добавить локальный или удаленный сервер MCP.

---

#### list

Перечислите все настроенные серверы MCP и состояние их подключения.

```bash
bugbee mcp list
```

Или используйте короткую версию.

```bash
bugbee mcp ls
```

---

#### auth

Аутентификация с помощью сервера MCP с поддержкой OAuth.

```bash
bugbee mcp auth [name]
```

Если вы не укажете имя сервера, вам будет предложено выбрать один из доступных серверов с поддержкой OAuth.

Вы также можете перечислить серверы с поддержкой OAuth и их статус аутентификации.

```bash
bugbee mcp auth list
```

Или используйте короткую версию.

```bash
bugbee mcp auth ls
```

---

#### logout

Удалите учетные данные OAuth для сервера MCP.

```bash
bugbee mcp logout [name]
```

---

#### debug

Отладка проблем с подключением OAuth для сервера MCP.

```bash
bugbee mcp debug <name>
```

---

### models

Перечислите все доступные модели от настроенных поставщиков.

```bash
bugbee models [provider]
```

Эта команда отображает все модели, доступные у настроенных вами поставщиков, в формате `provider/model`.

Это полезно для определения точного названия модели, которое будет использоваться в [вашем config](/docs/config/).

При желании вы можете передать идентификатор поставщика, чтобы фильтровать модели по этому поставщику.

```bash
bugbee models anthropic
```

#### Флаги

| Флаг                                    | Описание                                                                          |
| --------------------------------------- | --------------------------------------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Обновите кеш моделей на сайте models.dev.                                         |
| <nobr><code>{"--verbose"}</code></nobr> | Используйте более подробный вывод модели (включая метаданные, такие как затраты). |

Используйте флаг `--refresh` для обновления списка кэшированных моделей. Это полезно, когда к поставщику добавлены новые модели и вы хотите увидеть их в bugbee.

```bash
bugbee models --refresh
```

---

### run

Запустите bugbee в неинтерактивном режиме, передав приглашение напрямую.

```bash
bugbee run [message..]
```

Это полезно для создания сценариев, автоматизации или когда вам нужен быстрый ответ без запуска полного TUI. Например.

```bash "bugbee run"
bugbee run Explain the use of context in Go
```

Вы также можете подключиться к работающему экземпляру `bugbee serve`, чтобы избежать холодной загрузки сервера MCP при каждом запуске:

```bash
# Start a headless server in one terminal
bugbee serve

# In another terminal, run commands that attach to it
bugbee run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Флаги

| Флаг                                     | Короткий | Описание                                                                                         |
| ---------------------------------------- | -------- | ------------------------------------------------------------------------------------------------ |
| <nobr><code>{"--command"}</code></nobr>  |          | Команда для запуска, используйте сообщение для аргументов                                        |
| <nobr><code>{"--continue"}</code></nobr> | `-c`     | Продолжить последний сеанс                                                                       |
| <nobr><code>{"--session"}</code></nobr>  | `-s`     | Идентификатор сеанса для продолжения                                                             |
| <nobr><code>{"--fork"}</code></nobr>     |          | Разветвить сеанс при продолжении (используйте с `--continue` или `--session`)                    |
| <nobr><code>{"--share"}</code></nobr>    |          | Поделиться сеансом                                                                               |
| <nobr><code>{"--model"}</code></nobr>    | `-m`     | Модель для использования в виде поставщика/модели.                                               |
| <nobr><code>{"--agent"}</code></nobr>    |          | Агент для использования                                                                          |
| <nobr><code>{"--file"}</code></nobr>     | `-f`     | Файл(ы) для прикрепления к сообщению                                                             |
| <nobr><code>{"--format"}</code></nobr>   |          | Формат: по умолчанию (отформатированный) или json (необработанные события JSON).                 |
| <nobr><code>{"--title"}</code></nobr>    |          | Название сеанса (использует усеченное приглашение, если значение не указано)                     |
| <nobr><code>{"--attach"}</code></nobr>   |          | Подключитесь к работающему серверу bugbee (например, http://localhost:4096)                    |
| <nobr><code>{"--password"}</code></nobr> | `-p`     | Пароль базовой аутентификации (по умолчанию `BUGBEE_SERVER_PASSWORD`)                          |
| <nobr><code>{"--username"}</code></nobr> | `-u`     | Имя пользователя базовой аутентификации (по умолчанию `BUGBEE_SERVER_USERNAME` или `bugbee`) |
| <nobr><code>{"--dir"}</code></nobr>      |          | Каталог для выполнения или путь на удалённом сервере при подключении                             |
| <nobr><code>{"--variant"}</code></nobr>  |          | Вариант модели (уровень рассуждений для провайдера)                                              |
| <nobr><code>{"--thinking"}</code></nobr> |          | Показать блоки размышлений                                                                       |
| <nobr><code>{"--port"}</code></nobr>     |          | Порт локального сервера (по умолчанию случайный порт)                                            |

---

### serve

Запустите автономный сервер bugbee для доступа к API. Полный HTTP-интерфейс можно найти в [server docs](/docs/server).

```bash
bugbee serve
```

При этом запускается HTTP-сервер, который обеспечивает доступ API к функциям bugbee без интерфейса TUI. Установите `BUGBEE_SERVER_PASSWORD`, чтобы включить базовую аутентификацию HTTP (имя пользователя по умолчанию — `bugbee`).

#### Флаги

| Флаг                                     | Описание                                                      |
| ---------------------------------------- | ------------------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Порт для прослушивания                                        |
| <nobr><code>{"--hostname"}</code></nobr> | Имя хоста для прослушивания                                   |
| <nobr><code>{"--mdns"}</code></nobr>     | Включить обнаружение mDNS                                     |
| <nobr><code>{"--cors"}</code></nobr>     | Дополнительные источники браузера, позволяющие разрешить CORS |

---

### session

Управляйте сессиями bugbee.

```bash
bugbee session [command]
```

---

#### list

Перечислите все сеансы bugbee.

```bash
bugbee session list
```

##### Флаги

| Флаг                                      | Короткий | Описание                                  |
| ----------------------------------------- | -------- | ----------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`     | Ограничить N последних сеансов.           |
| <nobr><code>{"--format"}</code></nobr>    |          | Формат вывода: таблица или json (таблица) |

---

### stats

Покажите статистику использования токенов и затрат для ваших сеансов bugbee.

```bash
bugbee stats
```

#### Флаги

| Флаг                                    | Описание                                                                                                   |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Показать статистику за последние N дней (все время)                                                        |
| <nobr><code>{"--tools"}</code></nobr>   | Количество инструментов для отображения (все)                                                              |
| <nobr><code>{"--models"}</code></nobr>  | Показать разбивку по использованию модели (по умолчанию скрыто). Передайте номер, чтобы показать верхнюю N |
| <nobr><code>{"--project"}</code></nobr> | Фильтровать по проекту (все проекты, пустая строка: текущий проект)                                        |

---

### export

Экспортируйте данные сеанса в формате JSON.

```bash
bugbee export [sessionID]
```

Если вы не укажете идентификатор сеанса, вам будет предложено выбрать один из доступных сеансов.

---

### import

Импортируйте данные сеанса из файла JSON или URL-адреса общего ресурса bugbee.

```bash
bugbee import <file>
```

Вы можете импортировать из локального файла или URL-адреса общего ресурса bugbee.

```bash
bugbee import session.json
bugbee import https://opncd.ai/s/abc123
```

---

### web

Запустите автономный сервер bugbee с веб-интерфейсом.

```bash
bugbee web
```

При этом запускается HTTP-сервер и открывается веб-браузер для доступа к bugbee через веб-интерфейс. Установите `BUGBEE_SERVER_PASSWORD`, чтобы включить базовую аутентификацию HTTP (имя пользователя по умолчанию — `bugbee`).

#### Флаги

| Флаг                                     | Описание                                                      |
| ---------------------------------------- | ------------------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Порт для прослушивания                                        |
| <nobr><code>{"--hostname"}</code></nobr> | Имя хоста для прослушивания                                   |
| <nobr><code>{"--mdns"}</code></nobr>     | Включить обнаружение mDNS                                     |
| <nobr><code>{"--cors"}</code></nobr>     | Дополнительные источники браузера, позволяющие разрешить CORS |

---

### acp

Запустите сервер ACP (агент-клиентский протокол).

```bash
bugbee acp
```

Эта команда запускает сервер ACP, который обменивается данными через stdin/stdout с использованием nd-JSON.

#### Флаги

| Флаг                                     | Описание                    |
| ---------------------------------------- | --------------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Рабочий каталог             |
| <nobr><code>{"--port"}</code></nobr>     | Порт для прослушивания      |
| <nobr><code>{"--hostname"}</code></nobr> | Имя хоста для прослушивания |

---

### uninstall

Удалите bugbee и удалите все связанные файлы.

```bash
bugbee uninstall
```

#### Флаги

| Флаг                                        | Короткий | Описание                                   |
| ------------------------------------------- | -------- | ------------------------------------------ |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`     | Сохраняйте файлы конфигурации              |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`     | Храните данные сеанса и снимки             |
| <nobr><code>{"--dry-run"}</code></nobr>     |          | Покажите, что было бы удалено без удаления |
| <nobr><code>{"--force"}</code></nobr>       | `-f`     | Пропустить запросы подтверждения           |

---

### upgrade

Обновляет bugbee до последней версии или определенной версии.

```bash
bugbee upgrade [target]
```

Чтобы обновиться до последней версии.

```bash
bugbee upgrade
```

Для обновления до определенной версии.

```bash
bugbee upgrade v0.1.48
```

#### Флаги

| Флаг                                   | Короткий | Описание                                                  |
| -------------------------------------- | -------- | --------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`     | Используемый метод установки: local, npm, pnpm, bun, brew |

---

## Глобальные флаги

CLI bugbee принимает следующие глобальные флаги.

| Флаг                                       | Короткий | Описание                                   |
| ------------------------------------------ | -------- | ------------------------------------------ |
| <nobr><code>{"--help"}</code></nobr>       | `-h`     | Отобразить справку                         |
| <nobr><code>{"--version"}</code></nobr>    | `-v`     | Распечатать номер версии                   |
| <nobr><code>{"--print-logs"}</code></nobr> |          | Печать журналов в stderr                   |
| <nobr><code>{"--log-level"}</code></nobr>  |          | Уровень журнала (DEBUG, INFO, WARN, ERROR) |

---

## Переменные среды

bugbee можно настроить с помощью переменных среды.

| Переменная                            | Тип                 | Описание                                                                         |
| ------------------------------------- | ------------------- | -------------------------------------------------------------------------------- |
| `BUGBEE_AUTO_SHARE`                 | логическое значение | Автоматически делиться сеансами                                                  |
| `BUGBEE_GIT_BASH_PATH`              | строка              | Путь к исполняемому файлу Git Bash в Windows                                     |
| `BUGBEE_CONFIG`                     | строка              | Путь к файлу конфигурации                                                        |
| `BUGBEE_TUI_CONFIG`                 | строка              | Путь к файлу конфигурации TUI                                                    |
| `BUGBEE_CONFIG_DIR`                 | строка              | Путь к каталогу конфигурации                                                     |
| `BUGBEE_CONFIG_CONTENT`             | строка              | Встроенное содержимое конфигурации json                                          |
| `BUGBEE_DISABLE_AUTOUPDATE`         | логическое значение | Отключить автоматическую проверку обновлений                                     |
| `BUGBEE_DISABLE_PRUNE`              | логическое значение | Отключить удаление старых данных                                                 |
| `BUGBEE_DISABLE_TERMINAL_TITLE`     | логическое значение | Отключить автоматическое обновление заголовка терминала                          |
| `BUGBEE_PERMISSION`                 | строка              | Встроенная конфигурация разрешений json                                          |
| `BUGBEE_DISABLE_DEFAULT_PLUGINS`    | логическое значение | Отключить плагины по умолчанию                                                   |
| `BUGBEE_DISABLE_LSP_DOWNLOAD`       | логическое значение | Отключить автоматическую загрузку LSP-сервера                                    |
| `BUGBEE_ENABLE_EXPERIMENTAL_MODELS` | логическое значение | Включить экспериментальные модели                                                |
| `BUGBEE_DISABLE_AUTOCOMPACT`        | логическое значение | Отключить автоматическое сжатие контекста                                        |
| `BUGBEE_DISABLE_CLAUDE_CODE`        | логическое значение | Отключить чтение из `.claude` (подсказка + навыки)                               |
| `BUGBEE_DISABLE_CLAUDE_CODE_PROMPT` | логическое значение | Отключить чтение `~/.claude/CLAUDE.md`                                           |
| `BUGBEE_DISABLE_CLAUDE_CODE_SKILLS` | логическое значение | Отключить загрузку `.claude/skills`                                              |
| `BUGBEE_DISABLE_MODELS_FETCH`       | логическое значение | Отключить получение моделей из удаленных источников                              |
| `BUGBEE_FAKE_VCS`                   | строка              | Поддельный поставщик VCS для целей тестирования                                  |
| `BUGBEE_CLIENT`                     | строка              | Идентификатор клиента (по умолчанию `cli`)                                       |
| `BUGBEE_ENABLE_EXA`                 | логическое значение | Включить инструменты веб-поиска Exa                                              |
| `BUGBEE_SERVER_PASSWORD`            | строка              | Включить базовую аутентификацию для `serve`/`web`                                |
| `BUGBEE_SERVER_USERNAME`            | строка              | Переопределить имя пользователя базовой аутентификации (по умолчанию `bugbee`) |
| `BUGBEE_MODELS_URL`                 | строка              | Пользовательский URL-адрес для получения конфигурации модели                     |

---

### Экспериментальные функции

Эти переменные среды позволяют использовать экспериментальные функции, которые могут быть изменены или удалены.

| Переменная                                      | Тип                 | Описание                                               |
| ----------------------------------------------- | ------------------- | ------------------------------------------------------ |
| `BUGBEE_EXPERIMENTAL`                         | логическое значение | Включить экспериментальные функции под общим флагом    |
| `BUGBEE_EXPERIMENTAL_ICON_DISCOVERY`          | логическое значение | Включить обнаружение значков                           |
| `BUGBEE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | логическое значение | Отключить копирование при выборе в TUI                 |
| `BUGBEE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | число               | Таймаут по умолчанию для команд bash в мс              |
| `BUGBEE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | число               | Максимальное количество токенов вывода для ответов LLM |
| `BUGBEE_EXPERIMENTAL_FILEWATCHER`             | логическое значение | Включить просмотр файлов для всего каталога            |
| `BUGBEE_EXPERIMENTAL_OXFMT`                   | логическое значение | Включить форматтер oxfmt                               |
| `BUGBEE_EXPERIMENTAL_LSP_TOOL`                | логическое значение | Включить экспериментальный инструмент LSP              |
| `BUGBEE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | логическое значение | Отключить просмотрщик файлов                           |
| `BUGBEE_EXPERIMENTAL_EXA`                     | логическое значение | Включить экспериментальные функции Exa                 |
| `BUGBEE_EXPERIMENTAL_LSP_TY`                  | логическое значение | Включить TY LSP для файлов python                      |
| `BUGBEE_EXPERIMENTAL_PLAN_MODE`               | логическое значение | Включить режим плана                                   |
| `BUGBEE_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | логическое значение | Включить фоновые задачи субагентов                     |
| `BUGBEE_EXPERIMENTAL_EVENT_SYSTEM`            | логическое значение | Включить экспериментальную систему событий             |
| `BUGBEE_EXPERIMENTAL_NATIVE_LLM`              | логическое значение | Включить нативный путь запросов LLM                    |
| `BUGBEE_EXPERIMENTAL_PARALLEL`                | логическое значение | Включить параллельное выполнение веб-поиска            |
| `BUGBEE_EXPERIMENTAL_SCOUT`                   | логическое значение | Включить субагент Scout                                |
| `BUGBEE_EXPERIMENTAL_WORKSPACES`              | логическое значение | Включить поддержку рабочих областей                    |
