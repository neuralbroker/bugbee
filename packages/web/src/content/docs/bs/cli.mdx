---
title: CLI
description: Bugbee CLI opcije i naredbe.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

Bugbee CLI po defaultu pokreće [TUI](/docs/tui) kada se pokrene bez ikakvih argumenata.

```bash
bugbee
```

Ali takođe prihvata naredbe kao što je dokumentovano na ovoj stranici. Ovo vam omogućava programsku interakciju sa Bugbee.

```bash
bugbee run "Explain how closures work in JavaScript"
```

---

### tui

Pokrenite Bugbee terminalski korisnički interfejs.

```bash
bugbee [project]
```

#### Opcije

| Opcija                                   | Kratko | Opis                                                                     |
| ---------------------------------------- | ------ | ------------------------------------------------------------------------ |
| <nobr><code>{"--continue"}</code></nobr> | `-c`   | Nastavite posljednju sesiju                                              |
| <nobr><code>{"--session"}</code></nobr>  | `-s`   | ID sesije za nastavak                                                    |
| <nobr><code>{"--fork"}</code></nobr>     |        | Forkujte sesiju pri nastavku (koristiti sa `--continue` ili `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |        | Prompt za upotrebu                                                       |
| <nobr><code>{"--model"}</code></nobr>    | `-m`   | Model za korištenje u obliku provider/model                              |
| <nobr><code>{"--agent"}</code></nobr>    |        | Agent za korištenje                                                      |
| <nobr><code>{"--port"}</code></nobr>     |        | Port na kojem treba slušati                                              |
| <nobr><code>{"--hostname"}</code></nobr> |        | Hostname na kojem treba slušati                                          |

---

## Naredbe

Bugbee CLI takođe ima sljedeće naredbe.

---

### agent

Upravljajte Bugbee agentima.

```bash
bugbee agent [command]
```

---

### attach

Priključite terminal na već pokrenut Bugbee backend server pokrenut putem `serve` ili `web` naredbi.

```bash
bugbee attach [url]
```

Ovo omogućava korištenje TUI-ja sa udaljenim Bugbee backend-om. Na primjer:

```bash
# Start the backend server for web/mobile access
bugbee web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
bugbee attach http://10.20.30.40:4096
```

#### Opcije

| Opcija                                   | Kratko | Opis                                                                                          |
| ---------------------------------------- | ------ | --------------------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |        | Radni direktorij za pokretanje TUI-a                                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c`   | Nastavi posljednju sesiju                                                                     |
| <nobr><code>{"--session"}</code></nobr>  | `-s`   | ID sesije za nastavak                                                                         |
| <nobr><code>{"--fork"}</code></nobr>     |        | Forkuj sesiju prilikom nastavka (koristite sa `--continue` ili `--session`)                   |
| <nobr><code>{"--password"}</code></nobr> | `-p`   | Lozinka za osnovnu autentifikaciju (zadano: `BUGBEE_SERVER_PASSWORD`)                       |
| <nobr><code>{"--username"}</code></nobr> | `-u`   | Korisničko ime za osnovnu autentifikaciju (zadano: `BUGBEE_SERVER_USERNAME` ili `bugbee`) |

---

#### create

Kreirajte novog agenta s prilagođenom konfiguracijom.

```bash
bugbee agent create
```

Ova naredba će vas voditi kroz kreiranje novog agenta sa prilagođenim sistemskim promptom i konfiguracijom alata.

---

#### list

Navedite sve dostupne agente.

```bash
bugbee agent list
```

---

### auth

Naredba za upravljanje vjerodajnicama i prijavom za provajdere.

```bash
bugbee auth [command]
```

---

#### login

Bugbee pokreće lista provajdera na [Models.dev](https://models.dev), tako da možete koristiti `bugbee auth login` da konfigurirate API ključeve za bilo kojeg provajdera kojeg želite koristiti. Ovo je pohranjeno u `~/.local/share/bugbee/auth.json`.

```bash
bugbee auth login
```

Kada se Bugbee pokrene, učitava dobavljače iz datoteke vjerodajnica. I ako postoje neki ključevi definirani u vašim okruženjima ili `.env` fajl u vašem projektu.

---

#### list

Navodi sve autentifikovane dobavljače pohranjene u datoteci vjerodajnica.

```bash
bugbee auth list
```

Ili kratka verzija.

```bash
bugbee auth ls
```

---

#### logout

Odjavljuje vas s provajdera tako što ga briše iz datoteke vjerodajnica.

```bash
bugbee auth logout
```

---

### github

Upravljajte GitHub agentom za automatizaciju repozitorija.

```bash
bugbee github [command]
```

---

#### install

Instalirajte GitHub agenta u svoj repozitorij.

```bash
bugbee github install
```

Ovo postavlja neophodni tok rada GitHub Actions i vodi vas kroz proces konfiguracije. [Saznajte više](/docs/github).

---

#### run

Pokrenite GitHub agent. Ovo se obično koristi u GitHub Actions.

```bash
bugbee github run
```

##### Opcije

| Opcija                                | Opis                                   |
| ------------------------------------- | -------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub mock event za pokretanje agenta |
| <nobr><code>{"--token"}</code></nobr> | GitHub Personal Access Token           |

---

### mcp

Upravljajte Model Context Protocol (MCP) serverima.

```bash
bugbee mcp [command]
```

---

#### add

Dodajte MCP server svojoj konfiguraciji.

```bash
bugbee mcp add
```

Ova naredba će vas voditi kroz dodavanje lokalnog ili udaljenog MCP servera.

---

#### list

Navedite sve konfigurirane MCP servere i njihov status veze.

```bash
bugbee mcp list
```

Ili koristite kratku verziju.

```bash
bugbee mcp ls
```

---

#### auth

Autentifikujte se sa MCP serverom koji je omogućen za OAuth.

```bash
bugbee mcp auth [name]
```

Ako ne navedete ime servera, od vas će biti zatraženo da izaberete neki od dostupnih servera koji podržavaju OAuth.
Također možete navesti servere koji podržavaju OAuth i njihov status autentifikacije.

```bash
bugbee mcp auth list
```

Ili koristite kratku verziju.

```bash
bugbee mcp auth ls
```

---

#### logout

Uklonite OAuth vjerodajnice za MCP server.

```bash
bugbee mcp logout [name]
```

---

#### debug

Otklanjanje grešaka (debug) OAuth veze sa MCP serverom.

```bash
bugbee mcp debug <name>
```

---

### models

Navedite sve dostupne modele konfiguriranih provajdera.

```bash
bugbee models [provider]
```

Ova naredba prikazuje sve modele dostupne kod vaših konfiguriranih provajdera u formatu `provider/model`.
Ovo je korisno za pronalaženje tačnog naziva modela za korištenje u [vašoj konfiguraciji](/docs/config/).
Opciono možete proslijediti ID provajdera za filtriranje modela po tom dobavljaču.

```bash
bugbee models anthropic
```

#### Opcije

| Opcija                                  | Opis                                                                     |
| --------------------------------------- | ------------------------------------------------------------------------ |
| <nobr><code>{"--refresh"}</code></nobr> | Osvježite keš modela sa models.dev                                       |
| <nobr><code>{"--verbose"}</code></nobr> | Koristite detaljniji izlaz modela (uključuje metapodatke poput troškova) |

Koristite `--refresh` zastavicu da ažurirate keširanu listu modela. Ovo je korisno kada su novi modeli dodani provajderu i želite da ih vidite u Bugbee.

```bash
bugbee models --refresh
```

---

### run

Pokrenite Bugbee u neinteraktivnom modu tako što ćete direktno proslijediti prompt.

```bash
bugbee run [message..]
```

Ovo je korisno za skriptiranje, automatizaciju ili kada želite brz odgovor bez pokretanja punog TUI-ja. Na primjer:

```bash "bugbee run"
bugbee run Explain the use of context in Go
```

Također možete priključiti pokrenutu `bugbee serve` instancu kako biste izbjegli vrijeme hladnog pokretanja MCP servera pri svakom pokretanju:

```bash
# Start a headless server in one terminal
bugbee serve

# In another terminal, run commands that attach to it
bugbee run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Opcije

| Opcija                                   | Kratko | Opis                                                                                          |
| ---------------------------------------- | ------ | --------------------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |        | Naredba za pokretanje, koristite poruku za argumente                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c`   | Nastavite posljednju sesiju                                                                   |
| <nobr><code>{"--session"}</code></nobr>  | `-s`   | ID sesije za nastavak                                                                         |
| <nobr><code>{"--fork"}</code></nobr>     |        | Forkujte sesiju pri nastavku (koristiti sa `--continue` ili `--session`)                      |
| <nobr><code>{"--share"}</code></nobr>    |        | Podijelite sesiju                                                                             |
| <nobr><code>{"--model"}</code></nobr>    | `-m`   | Model za korištenje u obliku provider/model                                                   |
| <nobr><code>{"--agent"}</code></nobr>    |        | Agent za korištenje                                                                           |
| <nobr><code>{"--file"}</code></nobr>     | `-f`   | Fajlovi koje treba priložiti poruci                                                           |
| <nobr><code>{"--format"}</code></nobr>   |        | Format: default (formatiran) ili json (sirovi JSON događaji)                                  |
| <nobr><code>{"--title"}</code></nobr>    |        | Naslov sesije (koristi skraćeni prompt ako nije navedena vrijednost)                          |
| <nobr><code>{"--attach"}</code></nobr>   |        | Priključite na pokrenuti Bugbee server (npr. http://localhost:4096)                         |
| <nobr><code>{"--password"}</code></nobr> | `-p`   | Lozinka za osnovnu autentifikaciju (zadano: `BUGBEE_SERVER_PASSWORD`)                       |
| <nobr><code>{"--username"}</code></nobr> | `-u`   | Korisničko ime za osnovnu autentifikaciju (zadano: `BUGBEE_SERVER_USERNAME` ili `bugbee`) |
| <nobr><code>{"--dir"}</code></nobr>      |        | Direktorij za pokretanje, ili putanja na udaljenom serveru pri spajanju                       |
| <nobr><code>{"--variant"}</code></nobr>  |        | Varijanta modela (napor zaključivanja specifičan za provajdera)                               |
| <nobr><code>{"--thinking"}</code></nobr> |        | Prikaži blokove razmišljanja                                                                  |
| <nobr><code>{"--port"}</code></nobr>     |        | Port za lokalni server (zadano na nasumični port)                                             |

---

### serve

Pokrenite Bugbee headless server za API pristup. Pogledajte [server docs](/docs/server) za kompletan HTTP interfejs.

```bash
bugbee serve
```

Ovo pokreće HTTP server koji pruža API pristup funkcionalnosti Bugbee-a bez TUI interfejsa. Postavite `BUGBEE_SERVER_PASSWORD` da omogućite HTTP osnovnu auth (korisničko ime je zadano na `bugbee`).

#### Opcije

| Opcija                                   | Opis                                                  |
| ---------------------------------------- | ----------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Port na kojem treba slušati                           |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname na kojem treba slušati                       |
| <nobr><code>{"--mdns"}</code></nobr>     | Omogući mDNS otkrivanje                               |
| <nobr><code>{"--cors"}</code></nobr>     | Dodatni origin(i) pretraživača koji dozvoljavaju CORS |

---

### session

Upravljajte Bugbee sesijama.

```bash
bugbee session [command]
```

---

#### list

Navedite sve Bugbee sesije.

```bash
bugbee session list
```

##### Opcije

| Opcija                                    | Kratko | Opis                                   |
| ----------------------------------------- | ------ | -------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`   | Ograničenje na N najnovijih sesija     |
| <nobr><code>{"--format"}</code></nobr>    |        | Izlazni format: table ili json (table) |

---

### stats

Prikaži statistiku upotrebe tokena i troškova za vaše Bugbee sesije.

```bash
bugbee stats
```

#### Opcije

| Opcija                                  | Opis                                                                                                       |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Prikaži statistiku za zadnjih N dana (sva vremena)                                                         |
| <nobr><code>{"--tools"}</code></nobr>   | Broj alata za prikaz (svi)                                                                                 |
| <nobr><code>{"--models"}</code></nobr>  | Prikaži raščlambu korištenja modela (skriveno prema zadanim postavkama). Proslijedite broj za prikaz top N |
| <nobr><code>{"--project"}</code></nobr> | Filtriraj po projektu (svi projekti, prazan niz: trenutni projekt)                                         |

---

### export

Izvezite podatke sesije kao JSON.

```bash
bugbee export [sessionID]
```

Ako ne unesete ID sesije, od vas će biti zatraženo da odaberete neku od dostupnih sesija.

---

### import

Uvezite podatke sesije iz JSON datoteke ili Bugbee dijeljenog URL-a.

```bash
bugbee import <file>
```

Možete uvesti iz lokalne datoteke ili Bugbee dijeljenog URL-a.

```bash
bugbee import session.json
bugbee import https://opncd.ai/s/abc123
```

---

### web

Pokrenite Bugbee headless server sa web interfejsom.

```bash
bugbee web
```

Ovo pokreće HTTP server i otvara web pretraživač za pristup Bugbee-u preko web interfejsa. Postavite `BUGBEE_SERVER_PASSWORD` da omogućite HTTP osnovnu auth (korisničko ime je zadano na `bugbee`).

#### Opcije

| Opcija                                   | Opis                                                  |
| ---------------------------------------- | ----------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Port na kojem treba slušati                           |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname na kojem treba slušati                       |
| <nobr><code>{"--mdns"}</code></nobr>     | Omogući mDNS otkrivanje                               |
| <nobr><code>{"--cors"}</code></nobr>     | Dodatni origin(i) pretraživača koji dozvoljavaju CORS |

---

### acp

Pokrenite ACP (Agent Client Protocol) server.

```bash
bugbee acp
```

Ova naredba pokreće ACP server koji komunicira preko stdin/stdout koristeći nd-JSON.

#### Opcije

| Opcija                                   | Opis                        |
| ---------------------------------------- | --------------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Radni direktorij            |
| <nobr><code>{"--port"}</code></nobr>     | Port na kojem treba slušati |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname na kojem slušati   |

---

### uninstall

Deinstalirajte Bugbee i uklonite sve povezane datoteke.

```bash
bugbee uninstall
```

#### Opcije

| Opcija                                      | Kratko | Opis                                          |
| ------------------------------------------- | ------ | --------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`   | Sačuvajte konfiguracijske datoteke            |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`   | Sačuvajte podatke i snimke sesije             |
| <nobr><code>{"--dry-run"}</code></nobr>     |        | Pokažite šta bi bilo uklonjeno bez uklanjanja |
| <nobr><code>{"--force"}</code></nobr>       | `-f`   | Preskoči upite za potvrdu                     |

---

### upgrade

Ažurira Bugbee na najnoviju verziju ili određenu verziju.

```bash
bugbee upgrade [target]
```

Za nadogradnju na najnoviju verziju.

```bash
bugbee upgrade
```

Za nadogradnju na određenu verziju.

```bash
bugbee upgrade v0.1.48
```

#### Opcije

| Opcija                                 | Kratko | Opis                                                    |
| -------------------------------------- | ------ | ------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`   | Korišteni način instalacije; curl, npm, pnpm, bun, brew |

---

## Globalne opcije

Bugbee CLI prihvata sljedeće globalne zastavice.

| Opcija                                     | Kratko | Opis                                      |
| ------------------------------------------ | ------ | ----------------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h`   | Prikaži pomoć                             |
| <nobr><code>{"--version"}</code></nobr>    | `-v`   | Ispiši broj verzije                       |
| <nobr><code>{"--print-logs"}</code></nobr> |        | Ispis logova u stderr                     |
| <nobr><code>{"--log-level"}</code></nobr>  |        | Nivo logovanja (DEBUG, INFO, WARN, ERROR) |

---

## Varijable okruženja

Bugbee se može konfigurirati pomoću varijabli okruženja.

| Varijabla                             | Tip     | Opis                                                               |
| ------------------------------------- | ------- | ------------------------------------------------------------------ |
| `BUGBEE_AUTO_SHARE`                 | boolean | Automatski dijeli sesije                                           |
| `BUGBEE_GIT_BASH_PATH`              | string  | Putanja do Git Bash izvršne datoteke na Windows-u                  |
| `BUGBEE_CONFIG`                     | string  | Putanja do konfiguracijskog fajla                                  |
| `BUGBEE_TUI_CONFIG`                 | string  | Putanja do TUI konfiguracijskog fajla                              |
| `BUGBEE_CONFIG_DIR`                 | string  | Putanja do konfiguracijskog direktorija                            |
| `BUGBEE_CONFIG_CONTENT`             | string  | Inline json konfiguracijski sadržaj                                |
| `BUGBEE_DISABLE_AUTOUPDATE`         | boolean | Onemogući automatske provjere ažuriranja                           |
| `BUGBEE_DISABLE_PRUNE`              | boolean | Onemogući brisanje (pruning) starih podataka                       |
| `BUGBEE_DISABLE_TERMINAL_TITLE`     | boolean | Onemogući automatsko ažuriranje naslova terminala                  |
| `BUGBEE_PERMISSION`                 | string  | Inline json konfiguracija dozvola                                  |
| `BUGBEE_DISABLE_DEFAULT_PLUGINS`    | boolean | Onemogući podrazumijevane dodatke (plugins)                        |
| `BUGBEE_DISABLE_LSP_DOWNLOAD`       | boolean | Onemogući automatsko preuzimanje LSP servera                       |
| `BUGBEE_ENABLE_EXPERIMENTAL_MODELS` | boolean | Omogući eksperimentalne modele                                     |
| `BUGBEE_DISABLE_AUTOCOMPACT`        | boolean | Onemogući automatsko sažimanje konteksta                           |
| `BUGBEE_DISABLE_CLAUDE_CODE`        | boolean | Onemogući čitanje iz `.claude` (prompt + vještine)                 |
| `BUGBEE_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Onemogući čitanje `~/.claude/CLAUDE.md`                            |
| `BUGBEE_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Onemogući učitavanje `.claude/skills`                              |
| `BUGBEE_DISABLE_MODELS_FETCH`       | boolean | Onemogući dohvaćanje modela iz udaljenih izvora                    |
| `BUGBEE_FAKE_VCS`                   | string  | Lažni VCS provajder za potrebe testiranja                          |
| `BUGBEE_CLIENT`                     | string  | Identifikator klijenta (zadano na `cli`)                           |
| `BUGBEE_ENABLE_EXA`                 | boolean | Omogući Exa alate za web pretraživanje                             |
| `BUGBEE_SERVER_PASSWORD`            | string  | Omogući osnovnu autentifikaciju za `serve`/`web`                   |
| `BUGBEE_SERVER_USERNAME`            | string  | Poništi osnovno korisničko ime autentifikacije (zadano `bugbee`) |
| `BUGBEE_MODELS_URL`                 | string  | Prilagođeni URL za dohvaćanje konfiguracije modela                 |

---

### Eksperimentalno

Ove varijable okruženja omogućavaju eksperimentalne karakteristike koje se mogu promijeniti ili ukloniti.

| Varijabla                                       | Tip     | Opis                                                    |
| ----------------------------------------------- | ------- | ------------------------------------------------------- |
| `BUGBEE_EXPERIMENTAL`                         | boolean | Omogući eksperimentalne funkcije pod zbirnom zastavicom |
| `BUGBEE_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Omogući otkrivanje ikona                                |
| `BUGBEE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Onemogući kopiranje pri odabiru u TUI                   |
| `BUGBEE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Zadano vremensko ograničenje za bash naredbe u ms       |
| `BUGBEE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Maksimalni izlazni tokeni za LLM odgovore               |
| `BUGBEE_EXPERIMENTAL_FILEWATCHER`             | boolean | Omogući praćenje datoteka za cijeli direktorij          |
| `BUGBEE_EXPERIMENTAL_OXFMT`                   | boolean | Omogući oxfmt formatter                                 |
| `BUGBEE_EXPERIMENTAL_LSP_TOOL`                | boolean | Omogući eksperimentalni LSP alat                        |
| `BUGBEE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Onemogući praćenje datoteka                             |
| `BUGBEE_EXPERIMENTAL_EXA`                     | boolean | Omogući eksperimentalne Exa funkcije                    |
| `BUGBEE_EXPERIMENTAL_LSP_TY`                  | boolean | Omogući TY LSP za python datoteke                       |
| `BUGBEE_EXPERIMENTAL_PLAN_MODE`               | boolean | Omogući Plan mod                                        |
| `BUGBEE_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Omogući pozadinske zadatke subagenata                   |
| `BUGBEE_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Omogući eksperimentalni sistem događaja                 |
| `BUGBEE_EXPERIMENTAL_NATIVE_LLM`              | boolean | Omogući nativnu putanju LLM zahtjeva                    |
| `BUGBEE_EXPERIMENTAL_PARALLEL`                | boolean | Omogući paralelno izvršavanje web pretrage              |
| `BUGBEE_EXPERIMENTAL_SCOUT`                   | boolean | Omogući Scout subagenta                                 |
| `BUGBEE_EXPERIMENTAL_WORKSPACES`              | boolean | Omogući podršku za radne prostore                       |
