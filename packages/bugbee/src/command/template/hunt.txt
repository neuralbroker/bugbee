Run a defensive security hunt on ${path}.

1. Switch to hunt mindset (evidence-first, defense-only).
2. Call vuln_scan with include_secrets true on the project root.
3. List findings ordered by severity.
4. For the top critical/high items, open the files and validate source→sink with Read/Grep.
5. Mark obvious false positives; leave uncertain items as draft with clear evidence notes.
6. Summarize: counts by severity, top 5 real risks, recommended next patches.
7. Do not live-exploit anything. Do not print full secrets.

If the user specified a focus area, prioritize that surface (auth, uploads, SSRF, injection, secrets, etc.).
